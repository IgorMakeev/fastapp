pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh './deployment/scripts/deploy.sh'
            }
        }
        stage('Push') {
            steps {
                sh './deployment/scripts/deploy.sh'
            }
        }
    }
}
