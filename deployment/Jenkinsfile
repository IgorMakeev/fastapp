pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh './deployment/scripts/build.sh'
            }
        }
        stage('Push') {
            steps {
                sh './deployment/scripts/push.sh'
            }
        }
    }
}
