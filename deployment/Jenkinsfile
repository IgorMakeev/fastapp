pipeline {
    agent any

    environment {
        ENV        = 'staging'
        REGION     = 'eu-west-1'
        REPOSITORY = '903549323823.dkr.ecr.eu-west-1.amazonaws.com'
    }
    stages {
        stage('Build') {
            steps {
                sh './deployment/scripts/deploy.sh'
            }
        }
        stage('Push') {
            steps {
                sh './deployment/scripts/deploy.sh'
            }
        }
    }
}
